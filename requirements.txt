# Core pipeline
ultralytics>=8.1.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scikit-learn>=1.3.0
Pillow>=9.0
pyyaml>=6.0

# Analysis and reporting
pandas>=2.0.0
scipy>=1.10.0          # exact Wilcoxon; a sign-test fallback exists without it
matplotlib>=3.7.0

# Profiling (H4). psutil is required for CPU, memory, network and disk;
# pynvml supplies GPU power, memory and utilisation on NVIDIA hardware.
# Both degrade to "unmeasured" rather than to zero when absent.
psutil>=5.9.0
pynvml>=11.5.0

# Dataset acquisition
requests>=2.28.0
tqdm>=4.65.0
